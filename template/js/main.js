var $container = $('#content');$container.imagesLoaded( function () {   $container.masonry({   columnWidth: '.image-wrapper',   itemSelector: '.image-wrapper'   });}); // masonery sys
$(document).ready(function () {   $('#share-box-toggle').click(function(){      $('#hidden-share-box').slideToggle(200);   });   var windowWidth = $(window).width();   if(windowWidth < 768){      $('.cat-button').click(function() {         $('.categories-wrapper').slideToggle(200);      });   } else {      $('.cat-button').mouseenter(function() {       $(this).children('.categories-wrapper').stop().slideDown(500)      }).mouseleave(function() {          $(this).children('.categories-wrapper').stop().slideUp(500)      });   }    $(".cat-button").click(function (){    $(".featured").slideToggle(200);    }); // close featured tab when categories toggle button is clicked   $(document).click(function(e) {      var target = e.target;      if (!$(target).is('#share-box-toggle') && !$(target).parents().is('#share-box-toggle')) {      $('#hidden-share-box').slideUp(200);   } }); //close share-box when click in other place    $(".menu-item-has-children > a").click(function () {    $(this).next(".sub-menu").slideToggle(200); // #sub-menu toggle    }); // open sub-menus in main-menu    $(".toggle-menu-button").click(function (){    $("#main-menu").toggleClass('main-menu-active', 5000);    $(".logo-img").fadeToggle(250);    $(".bottom-bar").slideToggle(180);    });    $(".recommend-use").click(function (){    $(".recommend_form").slideToggle(200);    });    $(".toggle-email").click(function() {    $(".hidden-email").slideToggle(150);    });    $(document).click(function(e) {	    var target = e.target;	    if (!$(target).is('.toggle-email') && !$(target).parents().is('.email')) {	        $('.hidden-email').fadeOut(200);	    }	  }); //close search when click in other place    $(".toggle-phone").click(function() {
    $(".hidden-phone").slideToggle(150);    });    $(document).click(function(e) {	    var target = e.target;	    if (!$(target).is('.toggle-phone') && !$(target).parents().is('.phone')) {	        $('.hidden-phone').fadeOut(200);	    }	  }); //close search when click in other place
    $(".luci").click(function (){    $(".featured").toggleClass('featured-active', 5000);    var $container = $('#yogendra');      			$container.imagesLoaded(function(){      				$container.masonry('reloadItems');      				 $container.masonry({					    columnWidth: '.image-wrapper',					    itemSelector: '.image-wrapper'					  });      			});    });      $('.pull-down').each(function() {   	$(this).css('margin-top', $(this).parent().height()-$(this).height());      });   });   'use strict';   ;( function ( document, window, index )   {   	var inputs = document.querySelectorAll( '.inputfile' );   	Array.prototype.forEach.call( inputs, function( input )   	{   		var label	 = input.nextElementSibling,   			labelVal = label.innerHTML;   		input.addEventListener( 'change', function( e )   		{   			var fileName = '';   			if( this.files && this.files.length > 1 )   				fileName = ( this.getAttribute( 'data-multiple-caption' ) || '' ).replace( '{count}', this.files.length );   			else   				fileName = e.target.value.split( '\\' ).pop();   			if( fileName )   				label.querySelector( 'span' ).innerHTML = fileName;   			else   				label.innerHTML = labelVal;   		});   		// Firefox bug fix   		input.addEventListener( 'focus', function(){ input.classList.add( 'has-focus' ); });   		input.addEventListener( 'blur', function(){ input.classList.remove( 'has-focus' ); });   	});   }( document, window, 0 ));